syntax = "proto3";

import "scalapb/scalapb.proto";

option (scalapb.options) = {
  scope: FILE
  package_name: "calvin.protocol"
  flat_package: true
};

message Response {
    option (scalapb.message).extends = "calvin.Command";
    option (scalapb.message).companion_extends = "calvin.Command";

    bool ok = 1;
}